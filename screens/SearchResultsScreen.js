import React from 'react';
import { StyleSheet, Text, View, Button } from 'react-native';

const SearchResultsScreen = ({ navigation }) => {
  return <Text>Search Results Screen</Text>;
};

export default SearchResultsScreen;
